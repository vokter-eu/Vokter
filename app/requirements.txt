fastapi==0.115.*
uvicorn[standard]==0.32.*
httpx==0.27.*
pypdf==5.*
python-multipart>=0.0.18
faster-whisper>=1.0,<2
# TTS engine 1: Kokoro-82M (Apache-2.0) via kokoro-onnx — runs on the onnxruntime faster-whisper
# already pulls in, no PyTorch. Covers en/es/fr/it/pt.
kokoro-onnx>=0.4,<1
# TTS engine 2: Piper (MIT) — the languages Kokoro can't speak (de/nl/ca), as on-demand
# per-language voice packs. Same onnxruntime; ships piper-phonemize (its own espeak-ng data).
piper-tts==1.4.2
sqlcipher3>=0.5,<1
mcp>=1.0,<2
nostr-sdk>=0.44,<1
# OS keychain for the DB key (desktop 3.3-A): the frozen binary's --orchestrate
# mode reads GNOME Keyring in-process, so secretstorage (+jeepney, +cryptography)
# must live in the freeze env. Linux-only — Mac/Windows get their own keychain later.
secretstorage>=3.3,<4 ; sys_platform == "linux"
